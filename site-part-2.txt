;gap:10px;margin:18px 0 20px
    }
    .price strong{font-family:"Libre Baskerville",serif;font-size:4.1rem;letter-spacing:-.06em;color:var(--green)}
    .price span{color:var(--muted)}
    .price-card p{color:#58635e}
    .checks{list-style:none;padding:0;margin:28px 0 0}
    .checks li{padding:10px 0;border-top:1px solid var(--line);display:flex;gap:11px}
    .checks li::before{content:"✓";font-weight:700;color:var(--rust)}
    .note{
      margin-top:20px;font-size:.88rem;color:var(--muted);padding:15px 17px;
      border-left:3px solid var(--gold);background:#f1ebdf;border-radius:0 12px 12px 0
    }
    .included-card h3{margin-bottom:25px}
    .included-row{display:flex;justify-content:space-between;gap:20px;padding:18px 0;border-top:1px solid var(--line)}
    .included-row strong{font-size:.95rem}
    .included-row span{font-size:.9rem;color:var(--muted);text-align:right;max-width:55%}
    .hosting{
      padding:100px 0;background:var(--cream);
    }
    .hosting-grid{display:grid;grid-template-columns:1fr 1fr;gap:70px;align-items:center}
    .hosting-copy p{color:#56615d;font-size:1.08rem}
    .hosting-box{
      background:var(--paper);border:1px solid var(--line);padding:34px;border-radius:var(--radius)
    }
    .hosting-box h3{margin-bottom:10px}
    .hosting-box p{font-size:.96rem;color:#5c6762}
    .mini-points{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:24px}
    .mini-point{padding:15px;background:#fff;border:1px solid var(--line);border-radius:14px;font-size:.88rem}
    .story{background:var(--paper)}
    .story-grid{display:grid;grid-template-columns:.8fr 1.2fr;gap:75px;align-items:center}
    .portrait-frame{
      margin:0;min-height:470px;border-radius:28px;overflow:hidden;
      border:1px solid var(--line);background:#ddd6ca;box-shadow:var(--shadow);
    }
    .portrait-frame img{width:100%;height:100%;min-height:470px;object-fit:cover;object-position:center 27%;}

    .story-copy p{color:#56615d;font-size:1.08rem}
    .signature{font-family:"Libre Baskerville",serif;color:var(--green);font-size:1.05rem;margin-top:25px}
    .retainer{background:var(--green);color:#f7f2e8}
    .retainer-grid{display:grid;grid-template-columns:1fr 1fr;gap:70px;align-items:center}
    .retainer p{color:rgba(247,242,232,.74);font-size:1.05rem}
    .retainer-list{list-style:none;padding:0;margin:0}
    .retainer-list li{padding:17px 0;border-bottom:1px solid rgba(255,255,255,.15);display:flex;gap:14px}
    .retainer-list li::before{content:"+";color:#d9b86c;font-weight:700}
    .contact{padding:110px 0}
    .contact-grid{display:grid;grid-template-columns:.8fr 1.2fr;gap:75px}
    .contact-copy p{color:#5b6762;font-size:1.08rem}
    form{background:var(--paper);border:1px solid var(--line);padding:35px;border-radius:var(--radius)}
    .field{margin-bottom:17px}
    label{display:block;font-size:.78rem;text-transform:uppercase;letter-spacing:.1em;font-weight:700;margin-bottom:7px;color:#4e5b56}
    input,textarea,select{
      width:100%;border:1px solid #cec4b4;background:#fffdf8;border-radius:12px;
      padding:13px 14px;font:inherit;color:var(--ink);outline:none
    }
    input:focus,textarea:focus,select:focus{border-color:var(--green);box-shadow:0 0 0 3px rgba(24,53,46,.08)}
    textarea{min-height:130px;resize:vertical}
    .form-row{display:grid;grid-template-columns:1fr 1fr;gap:15px}
     .recipient-box{
      display:flex;align-items:center;justify-content:space-between;gap:16px;
      margin-top:18px;padding:13px 15px;border:1px solid var(--line);
      background:rgba(255,255,255,.58);border-radius:12px;color:#59645f;
    }
    .recipient-label{font-size:.72rem;letter-spacing:.08em;text-transform:uppercase;font-weight:700;color:#6c756f}
    .recipient-email{display:flex;align-items:center;gap:8px;color:var(--green);font-size:.9rem;font-weight:700;white-space:nowrap}
    .recipient-email svg{width:15px;height:15px;flex:0 0 15px}
    .form-status{margin-top:14px;font-size:.9rem;line-height:1.45;min-height:1.3em;color:var(--muted)}
    .form-status.success{color:var(--green);font-weight:600}
    .form-status.error{color:#985036;font-weight:600}
    .form-foot{display:flex;align-items:center;justify-content:space-between;gap:15px;margin-top:20px}
    .form-foot small{color:var(--muted)}
    .footer{background:#14211e;color:rgba(255,255,255,.65);padding:35px 0}
    .footer-inner{display:flex;justify-content:space-between;gap:30px;align-items:center}
    .footer-brand{color:#fff;font-family:"Libre Baskerville",serif}
    .footer small{font-size:.78rem}
    .fade{opacity:0;transform:translateY(16px);transition:opacity .7s ease,transform .7s ease}
    .fade.visible{opacity:1;transform:none}
    .menu-button{display:none;background:none;border:0;font-size:1.4rem;line-height:1;cursor:pointer;color:var(--green)}
    body{overflow-x:hidden}

    @media(max-width:850px){
      .container{width:min(100% - 32px,1120px)}
      .nav-inner{height:70px}
      .nav-links{
        display:none;
        position:absolute;
        top:70px;left:0;right:0;
        flex-direction:column;
        align-items:stretch;
        gap:0;
        padding:8px 16px 16px;
        background:rgba(251,248,241,.98);
        border-bottom:1px solid var(--line);
        box-shadow:0 16px 30px rgba(28,40,36,.08);
      }
      .nav-links.mobile-open{display:flex}
      .nav-links a{padding:12px 8px}
      .nav-links .nav-cta{margin:6px 0 0;text-align:center}
      .menu-button{display:inline-flex;align-items:center;justify-content:center;width:44px;height:44px;border-radius:50%}
      .hero-grid,.intro-grid,.offer-grid,.hosting-grid,.story-grid,.retainer-grid,.contact-grid{grid-template-columns:1fr;gap:42px}
      .hero{padding:56px 0 72px}
      .hero-copy p{font-size:1.05rem}
      .hero-actions{margin-top:28px}
      .logo-card{max-width:620px;width:100%;margin:auto;padding:28px 22px 48px;transform:none;border-radius:24px}
      .hero-wordmark strong{font-size:2.05rem}
      .hero-wordmark span{letter-spacing:.18em;font-size:.66rem}
      .logo-caption{left:22px;bottom:16px;font-size:.64rem}
      .process-grid{grid-template-columns:1fr}
      .portrait-frame{min-height:390px}
      .portrait-frame img{min-height:390px;object-position:center 25%}
      .section,.contact{padding:76px 0}
      .hosting{padding:78px 0}
      .footer-inner{flex-direction:column;align-items:flex-start}
      .included-row{flex-direction:column;gap:7px}
      .included-row span{max-width:none;text-align:left}