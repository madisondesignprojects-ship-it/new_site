<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Madison Design Projects builds thoughtful, affordable websites for local businesses. $400 flat, with a 3–5 day turnaround." />
  <meta name="theme-color" content="#18352e" />
  <title>Madison Design Projects — Websites for Local Businesses</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Libre+Baskerville:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --ink:#24312d;
      --green:#24453b;
      --green-2:#26483e;
      --paper:#f2ece0;
      --cream:#fcfaf5;
      --line:#d8cebd;
      --muted:#6d7772;
      --rust:#b36b46;
      --gold:#c8a15a;
      --blue:#7e9696;
      --shadow:0 20px 60px rgba(35,49,45,.11);
      --radius:22px;
    }
    *{box-sizing:border-box}
    html{scroll-behavior:smooth}
    body{
      margin:0;
      color:var(--ink);
      background:var(--cream);
      font-family:"DM Sans",system-ui,sans-serif;
      line-height:1.6;
    }
    a{color:inherit;text-decoration:none}
    img{max-width:100%;display:block}
    .container{width:min(1120px, calc(100% - 40px));margin:0 auto}
    .eyebrow{
      display:inline-flex;align-items:center;gap:9px;
      font-size:.74rem;letter-spacing:.14em;text-transform:uppercase;font-weight:700;
      color:var(--green);
    }
    .eyebrow::before{content:"";width:24px;height:1px;background:currentColor}
    h1,h2,h3,p{margin-top:0}
    h1,h2,h3{font-family:"Libre Baskerville",Georgia,serif;line-height:1.12}
    h1{font-size:clamp(3.1rem,7vw,6.3rem);letter-spacing:-.055em;margin-bottom:25px}
    h2{font-size:clamp(2.15rem,4.5vw,4rem);letter-spacing:-.04em}
    h3{font-size:1.25rem}
    .nav{
      position:sticky;top:0;z-index:20;
      background:rgba(251,248,241,.90);backdrop-filter:blur(14px);
      border-bottom:1px solid rgba(217,208,193,.8);
    }
    .nav-inner{height:76px;display:flex;align-items:center;justify-content:space-between}
    .brand{display:flex;align-items:center;gap:11px;font-weight:700;letter-spacing:-.02em}
    .brand-mark{width:46px;height:46px;object-fit:contain;flex:0 0 46px}
    .brand span{font-size:.98rem}
    .nav-links{display:flex;gap:28px;align-items:center;font-size:.92rem}
    .nav-links a{color:#45514d}
    .nav-links a:hover{color:var(--green)}
    .nav-cta{
      padding:10px 17px;border:1px solid var(--green);border-radius:999px;
      color:var(--green)!important;font-weight:700;
    }
    .hero{
      position:relative;overflow:hidden;
      padding:86px 0 95px;
      background:
        radial-gradient(circle at 88% 20%, rgba(200,161,90,.20), transparent 25%),
        radial-gradient(circle at 8% 85%, rgba(126,150,150,.14), transparent 24%),
        var(--paper);
      border-bottom:1px solid var(--line);
    }
    .hero-grid{display:grid;grid-template-columns:1.12fr .88fr;gap:70px;align-items:center}
    .hero-copy p{max-width:680px;font-size:1.14rem;color:#56625e}
    .accent{color:var(--rust)}
    .hero-actions{display:flex;gap:13px;flex-wrap:wrap;margin-top:34px}
    .button{
      display:inline-flex;align-items:center;justify-content:center;gap:9px;
      border-radius:999px;padding:14px 21px;font-weight:700;
      transition:.2s ease;cursor:pointer;border:0;font:inherit;
    }
    .button-primary{background:var(--green);color:#fff}
    .button-primary:hover{background:var(--green-2);transform:translateY(-2px)}
    .button-secondary{border:1px solid var(--line);background:rgba(255,255,255,.45);color:var(--ink)}
    .button-secondary:hover{border-color:#a9a093;transform:translateY(-2px)}
    .hero-note{display:flex;gap:25px;margin-top:32px;flex-wrap:wrap}
    .hero-note strong{display:block;font-size:1.05rem}
    .hero-note span{font-size:.83rem;color:var(--muted)}
    .logo-card{
      position:relative;background:#162521;border-radius:30px;padding:35px;
      box-shadow:var(--shadow);transform:rotate(1deg);
      overflow:hidden;
    }
    .logo-card::before{
      content:"";position:absolute;inset:0;
      background:radial-gradient(circle at 75% 15%,rgba(200,161,90,.23),transparent 32%),
                 radial-gradient(circle at 15% 85%,rgba(126,150,150,.18),transparent 30%);
      pointer-events:none;
    }
    .logo-lockup{position:relative;z-index:1;padding:8px 0 24px;text-align:center}
    .logo-lockup img{width:min(100%,470px);margin:auto;filter:drop-shadow(0 12px 24px rgba(0,0,0,.18))}
    .hero-wordmark{margin-top:-2px;color:#f7f2e8}
    .hero-wordmark strong{display:block;font-family:"Libre Baskerville",Georgia,serif;font-size:clamp(2.2rem,5vw,3.6rem);letter-spacing:.08em;line-height:.95}
    .hero-wordmark span{display:block;margin-top:12px;font-size:.79rem;letter-spacing:.28em;color:#d9cdbd}
    .logo-caption{
      position:absolute;left:28px;bottom:20px;color:rgba(255,255,255,.62);
      font-size:.7rem;letter-spacing:.13em;text-transform:uppercase
    }
    .section{padding:110px 0}
    .section-tight{padding:85px 0}
    .intro-grid{display:grid;grid-template-columns:.72fr 1.28fr;gap:80px;align-items:start}
    .intro-copy{font-size:1.16rem;color:#4e5a55;max-width:690px}
    .intro-copy p{margin-bottom:20px}
    .pullquote{
      margin:35px 0 0;padding:25px 0 0;border-top:1px solid var(--line);
      font-family:"Libre Baskerville",Georgia,serif;font-size:1.5rem;line-height:1.45;color:var(--green)
    }
    .process{background:var(--green);color:#f7f2e8}
    .process .eyebrow{color:#e3c788}
    .process-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px;margin-top:50px}
    .process-card{
      border:1px solid rgba(255,255,255,.17);border-radius:var(--radius);padding:30px;
      background:rgba(255,255,255,.045);
    }
    .process-number{font-family:"Libre Baskerville",serif;color:#d9b86c;font-size:1rem}
    .process-card h3{margin:22px 0 10px;color:#fff}
    .process-card p{color:rgba(247,242,232,.72);margin:0}
    .offer{background:#e8e0d1}
    .offer-grid{display:grid;grid-template-columns:1fr 1fr;gap:30px;align-items:stretch}
    .price-card,.included-card{
      background:var(--cream);border:1px solid var(--line);border-radius:var(--radius);padding:38px;
      box-shadow:0 10px 35px rgba(28,40,36,.05)
    }
    .price{
      display:flex;align-items:baseline